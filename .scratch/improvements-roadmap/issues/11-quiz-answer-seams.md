# Quiz-answer seams: ProcessAnswer, settings projection, DetectConfusion

Status: ready-for-agent
Category: enhancement
Area: backend-seam

## What to build

Three related backend seams that pull SM-2/quiz logic out of the handler and make it
directly testable. No SQL moves between files; SM-2 parameters are frozen (ADR-0002).

Covers plan items 5.1, 5.2, 5.3.

## Acceptance criteria

- [ ] **5.1** `sm2.ProcessAnswer(progress SM2Progress, correct bool) SM2Progress` exists and internally handles the learning-phase branch (`UpdateLearning` vs `Update`), the conditional `TotalAttempts` increment (skipped on graduation), and always applies `CalcStreakBonus` last; `QuizHandler.Answer()` is reduced to a single call. Unit tests cover correct-in-learning (graduation), wrong-in-learning (reset), correct-in-regular, wrong-in-regular
- [ ] **5.2** `UserSettings.QuizConfig() sm2.ProgressiveModeConfig` and `NewWordConfig() sm2.NewWordModeConfig` exist (placed to avoid an import cycle); `QuizHandler.Next()` uses them instead of manual field-mapping; the nil-settings default fallback is preserved; tests assert projection correctness and the nil/default path
- [ ] **5.3** `LookupConfusion` is renamed to `DetectConfusion` (same signature) everywhere; a `db` unit test asserts detection fires for a shared translation of a *different* entry, returns false for the correct entry, and false for an unknown answer; the `QuizHandler.Answer()` integration test still passes
- [ ] `go test ./... -count=1` passes

## Blocked by

None - can start immediately.
## Comments

> *This was generated by AI during triage.*

**Triage outcome:** `enhancement` + `ready-for-agent`.

Authored from the grilled `PLAN_IMPROVEMENTS.md`, where all product/deployment decisions were resolved. "What to build" and acceptance criteria are concrete and testable, so no `needs-info` round is required. Follow the repo TDD loop (failing test → minimal change → green) per `CLAUDE.md`.
