# Test coverage: E2E specs, JS unit helpers, split giant test files, untested packages

Status: ready-for-agent
Category: enhancement
Area: testing

## What to build

Close the remaining coverage gaps. None of these depend on a frontend module system —
JS unit tests use the existing inline-copy convention (accepted drift trade-off).

Covers plan items 6.1, 6.2, 6.3, 6.4.

## Acceptance criteria

- [ ] **6.1** Playwright specs added for stats, pinyin, mnemonics, and the mobile overlay UI (`e2e/*.spec.js`); `make test-e2e` green
- [ ] **6.2** JS unit tests for the pure helpers in `settings.js`, `pinyin.js`, `mnemonics.js`, `index.js` (inline-copy convention per CLAUDE.md); `npm test` green
- [ ] **6.3** `handlers_test.go` (5,767) and `db_test.go` (4,195) split by domain (`quiz_test.go`, `words_test.go`, …) with shared helpers; same package, no behaviour change
- [ ] **6.4** Coverage added for `email/`, `tts/`, and the four CLI import tools
- [ ] `go test ./... -count=1`, `npm test`, `make test-e2e` all pass

## Blocked by

None - can start immediately.
## Comments

> *This was generated by AI during triage.*

**Triage outcome:** `enhancement` + `ready-for-agent`.

Authored from the grilled `PLAN_IMPROVEMENTS.md`, where all product/deployment decisions were resolved. "What to build" and acceptance criteria are concrete and testable, so no `needs-info` round is required. Follow the repo TDD loop (failing test → minimal change → green) per `CLAUDE.md`.
