# Trade-off ADRs: handler-enforced isolation, session revocation, account lockout, single-host

Status: ready-for-agent
Category: enhancement
Area: documentation

## What to build

Record the deliberate, accepted security/architecture trade-offs as a single ADR so they
aren't re-litigated or mistaken for bugs. No code change.

Covers plan item 4.6 + the single-host note.

## Acceptance criteria

- [ ] An ADR records: `hmm_scenes` and `confusion_pairs` have no `user_id` and rely on handler-layer `GetWordByID` checks for isolation (verified NOT exploitable — defense-in-depth gap only)
- [ ] The ADR records the accepted stateless-HMAC session-revocation trade-off (logout clears the cookie; password-change revokes all sessions; no per-session denylist)
- [ ] The ADR records the accepted email-keyed account-lockout trade-off (no IP-scoped backoff)
- [ ] The ADR records the single-instance assumption: the daily new-word cap lives in handler memory under a mutex
- [ ] ADR added under `docs/adr/` following the existing format

## Blocked by

None - can start immediately.
## Comments

> *This was generated by AI during triage.*

**Triage outcome:** `enhancement` + `ready-for-agent`.

Authored from the grilled `PLAN_IMPROVEMENTS.md`, where all product/deployment decisions were resolved. "What to build" and acceptance criteria are concrete and testable, so no `needs-info` round is required. Follow the repo TDD loop (failing test → minimal change → green) per `CLAUDE.md`.
