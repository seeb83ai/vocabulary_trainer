# Tier classification: single Go source of truth

Status: ready-for-agent
Category: enhancement
Area: backend-seam

## What to build

The accuracy buckets (new, struggling, learning, practicing, mastered) are implemented
three times — SQL fragments in `tierFilter()`, the `wordTier()` Go switch, and a JS copy.
Unify the Go side; keep the JS copy but pin its boundaries with a Go table test.

Covers plan item 5.4.

## Acceptance criteria

- [ ] `sm2.Tier` type + constants (`TierNew`, `TierStruggling`, `TierLearning`, `TierPracticing`, `TierMastered`) defined in the `sm2` package
- [ ] `sm2.ClassifyTier(progress SM2Progress) Tier` implements the full boundary logic (0 attempts, learning flag, accuracy thresholds 0.50/0.70/0.85, attempt thresholds 3 and 10); `Tier.String()` returns the current display labels
- [ ] `handlers/quiz.go` `wordTier()` is deleted; callers use `sm2.ClassifyTier(...).String()`
- [ ] `tierFilter()` in `db/words.go` derives its SQL from the `Tier` constants so thresholds aren't restated in SQL strings
- [ ] Table-driven `sm2` tests assert every boundary; the JS copy stays but its expected boundaries are pinned by a Go test
- [ ] `go test ./... -count=1` passes

## Blocked by

None - can start immediately.
## Comments

> *This was generated by AI during triage.*

**Triage outcome:** `enhancement` + `ready-for-agent`.

Authored from the grilled `PLAN_IMPROVEMENTS.md`, where all product/deployment decisions were resolved. "What to build" and acceptance criteria are concrete and testable, so no `needs-info` round is required. Follow the repo TDD loop (failing test → minimal change → green) per `CLAUDE.md`.
