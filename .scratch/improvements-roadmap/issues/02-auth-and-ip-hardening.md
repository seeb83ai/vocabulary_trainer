# Auth & client-IP hardening: bcrypt timing, X-Real-IP, escHtml

Status: ready-for-agent
Category: enhancement
Area: security

## What to build

Close three small, decision-free hardening gaps: login user-enumeration via timing,
a spoofable client IP, and an `escHtml` gap for single-quoted attribute contexts.

Covers plan items 1.6, 1.9, 1.7.

## Acceptance criteria

- [ ] **1.6** Login runs a dummy `bcrypt.CompareHashAndPassword` on the `user == nil` path so the timing is the same whether or not the email exists (`handlers/auth.go:175-191`); test asserts the no-user path still does the compare
- [ ] **1.9** `ClientIP` prefers `X-Real-IP`, falls back to `RemoteAddr`, and **never** reads `X-Forwarded-For` (`handlers/ratelimit.go:91-104`); tests: `TestClientIP_PrefersXRealIP`, `TestClientIP_IgnoresXFF`, `TestClientIP_FallsBackToRemoteAddr`
- [ ] **1.9** README notes: deploy behind nginx that sets `X-Real-IP`; do not expose the binary directly
- [ ] **1.7** `escHtml` escapes `'` → `&#39;` (`frontend/app.js:135-141`); assertion added in `app.test.js`
- [ ] `go test ./... -count=1` and `npm test` pass

## Blocked by

None - can start immediately.
## Comments

> *This was generated by AI during triage.*

**Triage outcome:** `enhancement` + `ready-for-agent`.

Authored from the grilled `PLAN_IMPROVEMENTS.md`, where all product/deployment decisions were resolved. "What to build" and acceptance criteria are concrete and testable, so no `needs-info` round is required. Follow the repo TDD loop (failing test → minimal change → green) per `CLAUDE.md`.
