# Error-handling & logging hygiene: TTS errors, payload redaction, audio IDOR, answer-path errors

Status: ready-for-agent
Category: enhancement
Area: reliability

## What to build

Stop silently swallowing errors and leaking sensitive data in logs, and close the
cached-audio IDOR. All using the existing `log.Printf` idiom (no slog).

Covers plan items 1.3, 1.4, 1.5, 1.8.

## Acceptance criteria

- [ ] **1.3** Fire-and-forget TTS/audio goroutines capture and log errors with word-ID context (`handlers/words.go`, `handlers/upload_csv.go`); test asserts an error is logged on TTS failure
- [ ] **1.4** `llm/llm.go:373` no longer logs the full request payload — replaced with a redacted one-liner; test asserts payload contents are not logged
- [ ] **1.5** Audio IDOR fixed: `GetWordByID` ownership check runs **before** serving an existing cached `<id>.mp3`, not only in the lazy-generation branch (`handlers/audio.go:25-52`); `TestServeAudio_OtherUserForbidden`
- [ ] **1.8** Silently ignored errors in the answer path are at least logged with context (`handlers/quiz.go:457,459,481,496`); relevant handler test updated
- [ ] `go test ./... -count=1` passes

## Blocked by

None - can start immediately.
## Comments

> *This was generated by AI during triage.*

**Triage outcome:** `enhancement` + `ready-for-agent`.

Authored from the grilled `PLAN_IMPROVEMENTS.md`, where all product/deployment decisions were resolved. "What to build" and acceptance criteria are concrete and testable, so no `needs-info` round is required. Follow the repo TDD loop (failing test → minimal change → green) per `CLAUDE.md`.
