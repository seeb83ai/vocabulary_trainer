# CSV upload DoS limits + reflected upstream errors + Gemini key in URL

Status: ready-for-agent
Category: enhancement
Area: security

## What to build

Bound the CSV upload path against resource exhaustion, stop reflecting/logging full
upstream (DeepL/LLM) response bodies, and keep the Gemini API key out of URLs.

Covers plan items 2.1, 2.2, 2.3.

## Acceptance criteria

- [ ] **2.1** `http.MaxBytesReader` (≈8 MB, env-configurable) on the CSV body; row cap (≈5 000) returning 400 past the cap; route gains `.With(expensiveLimit)` (`main.go:228`); per-row TTS bounded by a worker-pool semaphore with dial/read deadlines (`tts/tts.go:44,74`); README documents the limits + env var. Tests: `TestUploadCSV_RejectsOversizedBody`, `TestUploadCSV_RejectsTooManyRows`
- [ ] **2.2** Client-visible errors are generic `writeError` messages; full DeepL/LLM bodies are kept in server logs only and no longer logged verbatim (`handlers/translate.go:93,104,230`, `llm/llm.go:382,362,170,324`, `handlers/llm.go:181,332`); test asserts the client error is generic
- [ ] **2.3** Gemini key moved out of the query string into a header if supported, or request URLs are guaranteed never logged (`llm/llm.go:285`)
- [ ] `go test ./... -count=1` passes; new route registered in `newRouter()` if changed

## Blocked by

None - can start immediately.
## Comments

> *This was generated by AI during triage.*

**Triage outcome:** `enhancement` + `ready-for-agent`.

Authored from the grilled `PLAN_IMPROVEMENTS.md`, where all product/deployment decisions were resolved. "What to build" and acceptance criteria are concrete and testable, so no `needs-info` round is required. Follow the repo TDD loop (failing test → minimal change → green) per `CLAUDE.md`.
