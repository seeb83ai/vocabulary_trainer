# Training-init in store + card scheduler seam

Status: ready-for-agent
Category: enhancement
Area: backend-seam

## What to build

Two seams that move quiz/word lifecycle logic into testable units. The card scheduler
depends on `ProcessAnswer` (issue 11) being merged first to simplify per-card state.

Covers plan items 5.5, 5.6.

## Acceptance criteria

- [ ] **5.5** `Store.CreateWord` with `StartTraining=true` atomically runs acknowledgement + component initialisation inside the transaction; the handler-side `initComponents` helper and the explicit `AcknowledgeWord` call in `Create` are deleted; handler only fires async TTS after. DB tests assert: `StartTraining=true` → non-null `first_seen_date` + component rows; `StartTraining=false` → null `first_seen_date` + no component rows
- [ ] **5.6** `SelectNextCard(candidates []CardCandidate) (CardCandidate, bool)` is a pure function returning the earliest-due candidate; `CardCandidate` carries `DueDate` + enough typed info to serialise vocab/mnemonic/component cards; `QuizHandler.Next()` builds candidates and delegates, deleting the nested if-chain. Unit tests cover single source, distinct due dates, tie-break (documented deterministic choice), all absent (`bool=false`); ≥1 integration test per card type
- [ ] `go test ./... -count=1` passes

## Blocked by

- 11-quiz-answer-seams.md (5.6 should follow 5.1 `ProcessAnswer`)
## Comments

> *This was generated by AI during triage.*

**Triage outcome:** `enhancement` + `ready-for-agent`, but **gated**: do not start until [11-quiz-answer-seams.md](11-quiz-answer-seams.md) is merged (5.6 `SelectNextCard` builds on the simplified per-card state from 5.1 `ProcessAnswer`). Spec is complete; follow the repo TDD loop per `CLAUDE.md`.
