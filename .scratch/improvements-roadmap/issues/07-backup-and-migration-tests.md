# Scheduled backup + migration-chain test coverage

Status: ready-for-agent
Category: enhancement
Area: reliability

## What to build

Close the biggest data-loss risk (manual-only backup) and raise migration-chain test
coverage so table-recreation migrations don't silently break upgrades.

Covers plan items 4.1, 4.2.

## Acceptance criteria

- [ ] **4.1** A systemd timer runs `sqlite3 .backup` nightly with retention; restore is documented and tested (`Makefile:64` is the current single manual target)
- [ ] **4.2** A test runs the full migration chain from an empty DB and from a mid-history snapshot (the table-recreation migrations v18/v21 are the risky ones) (`db/migrate_test.go`)
- [ ] `go test ./... -count=1` passes

## Note

Planned against the current `service/db/migrate.go`. Open PR #114 rewrites the migration
system; conflicts are handled at merge time, not pre-empted here.

## Blocked by

None - can start immediately.
## Comments

> *This was generated by AI during triage.*

**Triage outcome:** `enhancement` + `ready-for-agent`.

Authored from the grilled `PLAN_IMPROVEMENTS.md`, where all product/deployment decisions were resolved. "What to build" and acceptance criteria are concrete and testable, so no `needs-info` round is required. Follow the repo TDD loop (failing test → minimal change → green) per `CLAUDE.md`.
