# Schema cleanups: consolidate duplicate timestamps + transactional AcknowledgeRandomWords

Status: ready-for-agent
Category: enhancement
Area: reliability

## What to build

Remove a lockstep-update hazard and a partial-write hazard in the DB layer.

Covers plan items 4.3, 4.4.

## Acceptance criteria

- [ ] **4.3** The duplicate `first_seen_date` / `first_seen_at` columns on `sm2_progress` are consolidated to one (migration + update all readers, e.g. `db/quiz.go:102`, `db/words.go:729`); new-word cooldown behaviour unchanged, covered by a test
- [ ] **4.4** `AcknowledgeRandomWords` wraps its multi-statement writes in a single transaction (defer-Rollback / explicit-Commit) so a mid-loop failure leaves no partial state (`db/quiz.go:121-164`); regression test
- [ ] `go test ./... -count=1` passes

## Note

The 4.3 migration is planned against the current `service/db/migrate.go`; PR #114 conflicts
handled at merge time.

## Blocked by

None - can start immediately.
## Comments

> *This was generated by AI during triage.*

**Triage outcome:** `enhancement` + `ready-for-agent`.

Authored from the grilled `PLAN_IMPROVEMENTS.md`, where all product/deployment decisions were resolved. "What to build" and acceptance criteria are concrete and testable, so no `needs-info` round is required. Follow the repo TDD loop (failing test → minimal change → green) per `CLAUDE.md`.
