# CI quality gates: static analysis, coverage, README route-drift check

Status: ready-for-agent
Category: enhancement
Area: tooling

## What to build

Add the static-analysis and coverage that CI currently lacks, plus a check that keeps
the README API table honest.

Covers plan items 3.1, 3.2, 3.3.

## Acceptance criteria

- [ ] **3.1** A lint job in `.github/workflows/test.yml` runs `go vet` + golangci-lint/staticcheck + eslint + a gofmt check; a minimal `.golangci.yml` is added
- [ ] **3.2** `go test -cover` + vitest coverage run in CI (report-only, no gate)
- [ ] **3.3** Currently-undocumented routes are added to the README API table (`/api/quiz/accept-correct`, `/quiz/langs`, `/quiz/acknowledge-random`, `/quiz/advance`, `/words/{id}/review`, `/hmm/breakdown`); a CI script diffs `main.go` routes against the README
- [ ] Lint passes clean on the current tree (fix or justify any findings)
- [ ] `go test ./... -count=1` passes

## Blocked by

None - can start immediately.
## Comments

> *This was generated by AI during triage.*

**Triage outcome:** `enhancement` + `ready-for-agent`.

Authored from the grilled `PLAN_IMPROVEMENTS.md`, where all product/deployment decisions were resolved. "What to build" and acceptance criteria are concrete and testable, so no `needs-info` round is required. Follow the repo TDD loop (failing test → minimal change → green) per `CLAUDE.md`.
