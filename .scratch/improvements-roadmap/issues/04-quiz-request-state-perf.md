# Quiz request-state perf: settings double-load + session-validation cache

Status: ready-for-agent
Category: enhancement
Area: performance

## What to build

Remove two per-request redundant DB reads in the hot quiz path.

Covers plan items 1.10, 1.11.

## Acceptance criteria

- [ ] **1.10** User settings are loaded from the DB once per quiz round instead of twice (Next + Answer) — load once / cache per request (`handlers/quiz.go`); handler tests stay green, ideally asserting a single load
- [ ] **1.11** Session validation no longer does a `sessions_invalidated_at` DB lookup on every request — short-TTL in-memory cache, invalidated on password change (`handlers/auth.go`); test covers cache hit and invalidation-on-password-change
- [ ] `go test ./... -count=1` passes

## Blocked by

None - can start immediately.
## Comments

> *This was generated by AI during triage.*

**Triage outcome:** `enhancement` + `ready-for-agent`.

Authored from the grilled `PLAN_IMPROVEMENTS.md`, where all product/deployment decisions were resolved. "What to build" and acceptance criteria are concrete and testable, so no `needs-info` round is required. Follow the repo TDD loop (failing test → minimal change → green) per `CLAUDE.md`.
